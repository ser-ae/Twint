{
  "name": "reservehold-widget",
  "version": "0.3.0",
  "private": true,
  "description": "ReserveHold — restaurant table reservation widget with a TWINT/card no-show hold, plus its backend.",
  "scripts": {
    "dev": "node server/index.js",
    "seed": "node server/seed.js",
    "test": "npm run test:widget && npm run test:api",
    "test:widget": "node test/widget.test.js",
    "test:api": "node test/api.test.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "selfsigned": "^2.4.1"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
