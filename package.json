{
  "name": "reservehold-widget",
  "version": "0.2.0",
  "private": true,
  "description": "ReserveHold — restaurant table reservation widget with a TWINT/card no-show hold.",
  "scripts": {
    "test": "node test/widget.test.js"
  },
  "devDependencies": {
    "jsdom": "^24.0.0"
  }
}
