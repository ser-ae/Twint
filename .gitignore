node_modules/
package-lock.json
*.old-backup

# Local database, self-signed TLS keys and anything else the dev server
# generates. The private key in data/certs must never be committed.
data/
*.pem
*.db
*.db-wal
*.db-shm

.env
.env.local
